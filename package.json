{
  "name": "carl-code",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for Carl's pi-based agent harness: harness package, agent sidecar, and Tauri desktop GUI.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:gui": "bun run --cwd apps/gui dev",
    "dev:sidecar": "bun run --cwd packages/sidecar dev"
  }
}
