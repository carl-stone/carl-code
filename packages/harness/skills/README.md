# skills

Put your `SKILL.md` folders here. Each subfolder with a `SKILL.md` is a skill
that the agent can load as instruction context. This directory is auto-discovered
by pi when this repo is installed as a package.
