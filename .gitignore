# Node / package installs
node_modules/

# Agent state and local caches
.pi/
.carl-code/
*.local

# Secrets — never commit credentials
*.pem
.env
.env.*

# Bun
bun.lockb
*.tsbuildinfo

# Local dependency-upgrade recovery records (not runtime state)
.upgrade-backups/
